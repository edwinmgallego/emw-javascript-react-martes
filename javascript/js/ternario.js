/**
 * Los ternarios en JavaScript se refieren a la operación condicional ternaria o el operador ternario, que es una forma abreviada de una estructura condicional if-else. Este operador evalúa una condición y devuelve un valor si la condición es verdadera y otro valor si la condición es falsa.
 * 
 */


cuenta = -10;

let edad =22;

const mensaje1 = cuenta < 0 && "Hola" ;

document.write(mensaje);

let mensaje = edad >= 18 ? "Eres mayor de edad" : "Eres menor de edad";