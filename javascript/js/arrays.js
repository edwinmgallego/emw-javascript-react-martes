/**
 * 
 * El método map() en JavaScript es una función que permite crear un nuevo array a partir de otro, aplicando una función sobre cada uno de los elementos del array original. Es importante destacar que map() no modifica el array original; en su lugar, devuelve un nuevo array con los resultados de aplicar la función proporcionada a cada elemento.
 */

//const [, usario2, ,] = ["Pepe", "Juan", "Lucia", "Maria"];

document.write(usario2);

 const numeros = [1, 2, 3, 4];
document.write("<ul>");
numeros.map((numero) => numero + 1);
 document.write("</ul>");
document.write(numeros);
 document.write(nuevo);
